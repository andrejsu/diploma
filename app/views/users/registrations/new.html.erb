<div class="hero h-screen py-10 bg-base-200">
  <div class="hero-content flex-col-reverse lg:flex-row">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name),
                 html: { class: "card flex-shrink-0 w-full max-w-sm shadow-2xl bg-base-100" }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="card-body">

        <div class="form-control">
          <div class="label">
            <%= f.label :email, class: "label-text" %>
          </div>
          <%= f.email_field :email, autocomplete: "email", class: "input input-bordered" %>
        </div>

        <div class="form-control">
          <div class="label">
            <%= f.label :password, "Пароль", class: "label-text" %>
          </div>
          <%= f.password_field :password, autocomplete: "new-password", class: "input input-bordered" %>
          <label class="label">
            <% if @minimum_password_length %>
              <em class="label-text-alt">(<%= @minimum_password_length %> characters minimum)</em>
            <% end %>
          </label>
        </div>

        <div class="form-control">
          <div class="label">
            <%= f.label :password_confirmation, "Подтвердите пароль", class: "label-text" %>
          </div>
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "input input-bordered" %>
        </div>

        <div class="form-control mt-6">
          <%= f.submit "Зарегистрироваться", class: 'btn btn-primary' %>
        </div>

        <div class="flex form-control text-center">
          <%= render "users/shared/links" %>
        </div>
      </div>
    <% end %>

    <div class="text-center">
      <h1 class="text-5xl font-bold">Присоединяйтесь к нашему сообществу!</h1>
      <p class="py-6">Зарегистрируйтесь, чтобы получить получить доступ к фунциям нашего приложения</p>
    </div>
  </div>
</div>